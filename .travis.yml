language: python
sudo: false
dist: bionic
python:
  - "3.7"

install:
  - python -m pip install pytest-cov codecov coveralls

# tests
script:
  - python -m pytest -v

after_success:
- codecov
- coveralls
